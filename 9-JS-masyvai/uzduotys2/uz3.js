let projects = ['kelioniu projektas', 'parduotuves projektas', 'automobiliu projektas', 'ledu projektas'];

for (let i = 0; i < projects.length; i++) {
    console.log(`${i + 1} projektas: ${projects[i]}`);
}